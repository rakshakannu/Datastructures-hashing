// given an array of elements, count the distinct elements in the array
// time = O(n), space = O(n)

// naive solution
n sqaure, nested loop

// efficient solution
// use a hash set
// the idea that hashset ignore the adding of the element if it is already present

int countDist(int[] arr, int n)
{
  HashSet<Integer> s = new HashSet<Integer>();
  
  for(int i=0;i<n;i++)
    s.add(arr[i]);
  
  return s.size();
}

time = O(1)
